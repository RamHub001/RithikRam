version: "3.9"
services:
  string_calculator_app:
    build: .
    container_name: string_calculator_app
    volumes:
      - ./string_calculator:/app
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true
